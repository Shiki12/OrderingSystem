<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.dao.ProductDao">
    <insert id="addProduct" parameterType="product">
        insert into product
            (name, price,zan,number,imageurl, miaoshu,cid,bid)
        VALUES  (#{name},#{price} ,#{zan}, #{number},#{imageUrl}, #{miaoshu},#{cid},#{bid})
    </insert>
    <update id="update" parameterType="product">
        update product set name = #{name},
        price = #{price},
        zan = #{zan},
        number = #{number},
        status = #{status},
        imageurl = #{imageUrl},
        miaoshu = #{miaoshu}
        where id = #{id};

    </update>
    <delete id="deleteById">
        delete
        from product
        where id = #{ id };

    </delete>

    <select id="getAll" resultType="com.web.entity.Product">

       select  * from  product

    </select>
    <select id="getById" resultType="com.web.entity.Product">
        select *
        from product where id = #{id};
    </select>
</mapper>