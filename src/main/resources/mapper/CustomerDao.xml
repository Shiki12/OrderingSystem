<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.dao.CustomerDao">

    <insert id="addCustomer">
        insert into customer (id, name, password,address,phone,status)
        values (#{id},#{name},#{password},#{address},#{phone},#{token});
    </insert>
    <update id="update">
        update customer
        set name = #{mame},
            password = #{password},
            address = #{address},
            phone = #{phone},
            status = #{token}
        where id = #{id};

    </update>
    <delete id="deleteById">
        delete
        from customer
        where id = #{id};

    </delete>


    <select id="getAll" resultType="customer">
        select * from customer;
    </select>
</mapper>